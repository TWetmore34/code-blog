  {{!-- add SELECTED comment here - do them by id so that you have a smaller ui
   and you dont run into the same each loop issue --}}
       <div class="card-header bg-new">
        <h3>Update Comment</h3>
    </div>
        <div id="email-parent" class="card-body">
            <form action="" class="">
                <label class="left" for="post-title">
                    <h3>Comment</h3>
                </label>
                <input value="{{comment.content}}" data-comment="{{comment.id}}" id="comment" class="w-75 block mx-auto my-2" type="text">
                <button id="update" class="btn btn-primary mx-auto d-block">Update</button>
               </form>
        </div>
<script src="/js/updateComment.js"></script>